{% set language = instance.repo_language | default('') %}
<repository language="{{ language }}" path="{{ workspace_path }}" />

<issue>
{{ instance.problem_statement }}
</issue>

<task>
Fix this issue with the smallest possible change to non-test files in {{ workspace_path }}.
</task>
